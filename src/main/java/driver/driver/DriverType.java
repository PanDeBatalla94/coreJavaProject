package driver.driver;

/**
 * Enum for the org.fundacionjala.core.driver types.
 */
public enum DriverType {
    CHROME,
    FIREFOX,
    DOCKER_CHROME,
    DOCKER_FIREFOX,
    SAUCELABS,
    BROWSER_STACK
}
